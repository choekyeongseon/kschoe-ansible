---
- name: Installing packages   
  hosts: localhost            
  
  tasks:                      
  - name: Install apache
    dnf: 
      name: httpd
      state: present

  - name: Start apache
    service:
      name: httpd
      state: started
      enabled: yes
